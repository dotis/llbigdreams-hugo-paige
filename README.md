A github action to build & deploy to github pages is in .github/workflows, but is currently nonfunctional.

Run dev server:
```bash
hugo serve -D --disableFastRender --logLevel debug --ignoreCache --printPathWarnings --printUnusedTemplates
```
